# Backend API (no trailing slash)
# VITE_API_URL=http://localhost:8000

# Yandex Metrika counter ID (digits only), from https://metrika.yandex.ru/
# Required at build time on Railway (Variables → rebuild frontend).
# VITE_METRIKA_ID=12345678
